[project]
name = "dagger_orchestrater"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-yaml>=1.4.0",
    "pydantic>=2.10.4",
    "dagger-io>=0.15.1",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "pytest-cov>=6.0.0",
    "pytest>=8.3.4",
    "ruff>=0.8.4",
]

[tool.uv.sources]
dagger-io = { path = "sdk", editable = true }
