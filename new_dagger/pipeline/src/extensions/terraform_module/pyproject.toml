[project]
name = "dagger-terraform-module"
version = "0.1.0"
description = "Terraform module for Dagger pipeline"
dependencies = [
    "pydantic>=2.0.0",
    "dagger-io>=0.15.1"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build" 