[project]
name = "dagger-ansible-module"
version = "0.1.0"
description = "Ansible module for Dagger pipeline"
dependencies = [
    "pydantic>=2.0.0",
    "dagger-io>=0.15.1"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build" 